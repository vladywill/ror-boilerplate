<!DOCTYPE html>
<html>
  <head>
    <title>SaasRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <div class="container mx-auto p-5 l-64">
      <% if user_signed_in? %>
          <%= render "layouts/menu" %>
      <% end %>
      <% if notice %>
        <p class="bg-blue-100 border border-blue-500 text-blue-700 px-4 py-3 rounded-lg mb-2" role="alert"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="bg-blue-100 border border-blue-500 text-blue-700 px-4 py-3 rounded-lg mb-2" role="alert"><%= alert %></p>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
